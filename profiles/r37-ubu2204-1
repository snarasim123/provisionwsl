#common (between powershell and bash) params
export distro_type=ubuntu
#name for the instance to be
export distro_name=r37-ubu2204-1

#powershell specific vars

# leave this empty for download. or setup a rootfs .tar file and point to the file.
export ps_distro_source=
# where the wsl installation files are installed
export ps_install_dir=C:\Users\snara\wsl
# id to lookup from list of known cloud images from csv file
export ps_distro_id=ubuntu2204


# cleanup this var
export default_user=snarasim

# cleanup the following vars/names
export code_base=/mnt/d/code
export code_root=setup
export code_src="$code_base/$code_root"

# cleanup the following var
export run_as_user=root

export upgrade=true

export skipsteps=packages,cloud,editor,git,container

# nvim,vim
# aws,cloud
# helm,k8s,k9s,container
